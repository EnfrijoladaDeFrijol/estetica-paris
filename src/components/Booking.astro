<section id="reservas" class="relative py-28 md:py-36 px-6 overflow-hidden section-glow-top">
    
    <div class="max-w-4xl mx-auto relative z-10 text-center">
        
        <div data-aos="fade-up">
            <h2 class="text-4xl md:text-5xl font-bold mb-14 text-gradient-accent">Reserva tu Espacio</h2>
        </div>

        <!-- Panel Glass del Widget -->
        <div class="glass-panel p-3 md:p-4 rounded-3xl relative overflow-hidden group mx-auto max-w-3xl" data-aos="zoom-in" data-aos-duration="1000">
            
            <!-- Shimmer -->
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-[rgba(212,175,55,0.08)] to-transparent -translate-x-full group-hover:animate-[shimmer_2s_infinite] pointer-events-none z-20"></div>

            <!-- Contenedor del Calendario (Altura ajustada a h-[600px] para mejor experiencia de usuario) -->
            <div class="rounded-2xl w-full h-[600px] relative z-10 overflow-hidden" style="background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.05);">
                
                <!-- Iframe de Setmore incrustado -->
                <iframe 
                    src="https://esteticaparis.setmore.com/926a54c2-88c3-4943-8cda-82c188dd0515" 
                    class="absolute inset-0 w-full h-full border-0 bg-transparent rounded-2xl"
                    scrolling="yes"
                    loading="lazy">
                </iframe>

            </div>
        </div>

    </div>

    <style>
        @keyframes shimmer {
            100% { transform: translateX(100%); }
        }
    </style>
</section>